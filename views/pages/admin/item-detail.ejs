<%- include('../../components/header.ejs') %>
<body id="page-top">
  <!-- Page Wrapper -->
  <div id="wrapper">
    <%- include('../../components/sidebar.ejs') %>
    <div id="content-wrapper" class="d-flex flex-column">
      <div id="content">
        <%- include('../../components/navbar.ejs') %>

        <!-- Begin Page Content -->
        <div class="container-fluid">
          <%- include('../../components/message.ejs') %>

          <!-- Page Heading -->
          <h1 class="h3 mb-4 text-gray-800">Detail Item</h1>
          <!-- End of Page Heading -->

            <ul class="nav nav-tabs" id="myTab" role="tablist">
              <li class="nav-item">
                <a class="nav-link active" id="show-feature-tab" data-toggle="tab" href="#show-feature" role="tab" aria-controls="show-feature" aria-selected="true">
                  Show Feature
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" id="show-activity-tab" data-toggle="tab" href="#show-activity" role="tab" aria-controls="show-activity" aria-selected="false">
                  Show Activity
                </a>
              </li>
            </ul>
            <div class="tab-content" id="myTabContent">
              <%- include('../../components/admin/item-detail-show-feature.ejs') %>
              <%- include('../../components/admin/item-detail-show-activity.ejs') %>
            </div>

        </div>
        <!-- /.container-fluid -->

      </div>
      <%- include('../../components/footer.ejs') %>
    </div>
  </div>
  <!-- End of Page Wrapper -->

  <!-- Scroll to Top Button-->
  <a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
  </a>

  <!-- Logout Modal-->
  <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
          <button class="close" type="button" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
          </button>
        </div>
        <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
        <div class="modal-footer">
          <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
          <a class="btn btn-primary" href="login.html">Logout</a>
        </div>
      </div>
    </div>
  </div>

<%- include('../../components/admin/item-detail-edit-feature.ejs') %>
<%- include('../../components/admin/item-detail-edit-activity.ejs') %>
<%- include('../../components/script.ejs') %>
<script>
  $(document).ready(function(){
    $('#dataTableFeature').DataTable();
    $('#dataTableActivity').DataTable();
  })

  $('#dataTableFeature').on('click','.button-update', function(){
    let id = $(this).data('id');
    let name = $(this).data('name');
    let quantity = $(this).data('quantity');
    $('#edit-feature-modal').modal('show');
    $('.id').val(id);
    $('.name').val(name);
    $('.quantity').val(quantity);
  })

  $('#dataTableActivity').on('click','.button-update', function(){
    let id = $(this).data('id');
    let name = $(this).data('name');
    let type = $(this).data('type');
    $('#edit-activity-modal').modal('show');
    $('.id').val(id);
    $('.name').val(name);
    $('.type').val(type);
  })
</script>
